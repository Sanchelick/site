<!DOCTYPE html>
<html>
  <head>
    <title>Snova</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css'%>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
      <h4><%= link_to "Хлебальник", root_path, :method => :get,  class: "p-2 text-dark"%></h4>
      <div class = "aktivity" align="right">
        <%= button_to "Зарегистрироваться", sing_up_path, class: "btn btn-outline-success", :method => :get%>
      <%= button_to "Войти", sing_in_path, class: "btn btn-outline-success", :method => :get%></div>
    </div>
    <%= yield %>
  </body>
</html>
